<h1>Edit Discussion</h1>
<%= turbo_frame_tag dom_id(@discussion), class: "w-full card items-center" do %>
  <%= render "form", discussion: @discussion %>
  <%= link_to "Cancel", @discussion, class: "btn" %>
<% end %>
